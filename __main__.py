from l2gen_processor import L2genProcessor
from ocsmart_processor import OCSMARTProcessor
from exctract_nc import ExtractorSeaDAS
from extract_h5 import OCSMARTExtractor
from extract_h5_l2 import SGLIL2Extractor

if __name__ == "__main__":
    # token = "TOKEN"
    # account = "USERNAME"
    # password = PASSWORD
    # in_path = "/Users/salah/workspace/data/SGLI-matchups/data.csv" ## contains ids
    # out_path = "/Volumes/PortableSSD/sgli-lvl1" ## path to save files
    # processor = Downloader(in_path, out_path, token, account, password)
    # processor.download_l1B(from_index=0)s
    # p = L2genProcessor("/home/shared/Data/SGLI/sgli-lvl1",
    #             "./input_data.csv", "./output_data.csv")
    # p.start(start_index=0)

    # ex = OCSMARTExtractor("/home/muhammad/SGLI/ORIGINAL_WITH_L1_IDs/test.csv", "/home/muhammad/SGLI/ocsmart_processed/test.csv")
    # ex = ExtractorSeaDAS("/home/muhammad/SGLI/ORIGINAL_WITH_L1_IDs/test.csv", "/home/muhammad/SGLI/seadas_processed/test.csv")
    # ex = SGLIL2Extractor("/home/muhammad/SGLI/ORIGINAL_WITH_L1_IDs/test.csv", "/home/muhammad/SGLI/l2_std/test.csv")
    # ex.start()
    print()
